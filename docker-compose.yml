services:
  coco:
    build: .
    image: faultline:latest
    container_name: faultline-coco
    user: "${PUID:-1000}:${PGID:-1003}"
    command: ["-config", "/config/config.toml"]
    stop_grace_period: 3m
    environment:
      - TZ=America/New_York
    group_add:
      - "${DOCKER_GID:-986}"
    ports:
      # HTML publishing harness — Cloudflare Tunnel via host.docker.internal
      - "8751:8751"
    volumes:
      - /home/camilovalderruten/faultline/coco.config.toml:/config/config.toml:ro
      - /home/camilovalderruten/.faultline/agents/coco:/agent:rw
      - /home/camilovalderruten/.faultline/shared/memory:/agent/memory/shared:rw
      - /var/run/docker.sock:/var/run/docker.sock
      - /home/camilovalderruten/.faultline/agents/coco/sandbox:/home/camilovalderruten/.faultline/agents/coco/sandbox:rw
    restart: unless-stopped

  arlo:
    build: .
    image: faultline:latest
    container_name: faultline-arlo
    user: "${PUID:-1000}:${PGID:-1003}"
    command: ["-config", "/config/config.toml"]
    stop_grace_period: 3m
    environment:
      - TZ=America/New_York
    group_add:
      - "${DOCKER_GID:-986}"
    ports:
      # HTML publishing harness — Cloudflare Tunnel via host.docker.internal
      - "8750:8750"
      # Local-voice turn API — loopback only, USB headset sidecar
      - "127.0.0.1:8760:8760"
    volumes:
      - /home/camilovalderruten/faultline/arlo.config.toml:/config/config.toml:ro
      - /home/camilovalderruten/.faultline/agents/arlo:/agent:rw
      - /home/camilovalderruten/.faultline/shared/memory:/agent/memory/shared:rw
      - /home/camilovalderruten/.faultline/agents/arlo/sandbox:/home/camilovalderruten/.faultline/agents/arlo/sandbox:rw
      - /var/run/docker.sock:/var/run/docker.sock
    restart: unless-stopped

  arlo-headset:
    build:
      context: .
      dockerfile: docker/headset/Dockerfile
    image: faultline-headset:latest
    container_name: faultline-arlo-headset
    network_mode: host
    group_add:
      - "29"
    devices:
      - /dev/snd
    env_file:
      - /home/camilovalderruten/.faultline/agents/arlo/headset.env
    environment:
      - TZ=America/New_York
      - ALSA_CARD=Headset
      - TURN_URL=http://127.0.0.1:8760
      - WAKE_MODEL=alexa
    depends_on:
      - arlo
    restart: unless-stopped
